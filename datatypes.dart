void main() {
  //String name = " Sneha ";

  //space haeu remove garsa (trim length)
  // print(name.trim().length);

  // print(name.endsWith(''));

  // print(name.contains(''));

  // print(name.substring(0, 3)); //break garxa

  // print(name.toLowerCase()); //lower case ma lekhxa

  //numerical integer

  int age = 17;
  int qty = 14;
  double price = 12.34;
  print(price.round());
}
